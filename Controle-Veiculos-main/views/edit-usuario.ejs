<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Editar Usuário</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2>Editar Usuário</h2>
    <a href="/usuarios" class="btn btn-secondary mb-3">&larr; Voltar</a>

    <!-- Alerta irreversível -->
    <div class="alert alert-warning">
      <strong>Atenção:</strong> Alterar os dados do usuário é uma operação <strong>irreversível</strong>.  
      Tem certeza de que deseja continuar?
    </div>

    <% if (erros.length) { erros.forEach(e => { %>
      <div class="alert alert-danger"><%= e.msg %></div>
    <% }) } %>

    <form method="POST" action="/usuarios/<%= usuario.id %>/edit">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <div class="mb-3">
        <label for="email" class="form-label">E-mail</label>
        <input type="email" class="form-control" id="email" name="email"
               value="<%= usuario.email %>" required>
      </div>

      <div class="mb-3">
        <label for="role" class="form-label">Role</label>
        <select class="form-select" id="role" name="role" required>
          <option value="user" <%= usuario.role==='user'?'selected':'' %>>Usuário</option>
          <option value="admin" <%= usuario.role==='admin'?'selected':'' %>>Administrador</option>
        </select>
      </div>

      <button type="submit"
              class="btn btn-danger"
              onclick="return confirm('Esta alteração é irreversível. Deseja realmente salvar?');">
        Salvar Alterações
      </button>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

